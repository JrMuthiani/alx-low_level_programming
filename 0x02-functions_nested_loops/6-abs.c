#include "main.h"

/**
 * _abs - Entry point
 * @j: takes a value
 * Description: compute absolute value of integer
 * Return: int
 */

int _abs(int j)
{
	if (j < 0)
	{
		return (-j);
	}
	else if (j > o)
	{
		return (j);
	}
	else
	{
		return (j);
	}
}
