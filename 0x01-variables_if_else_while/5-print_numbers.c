#include <stdio.h>

/**
 * main - Entry point
 * Description: Print single numbers with base 10
 * Return: 0
 */

int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
	{
		putchar(i);
	}
	putchar('\n');
	return (0);
}
